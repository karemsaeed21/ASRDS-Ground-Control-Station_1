# Models

Drop your trained YOLO human-detection weights here, e.g. `human_detector.pt`.

Point `detection.model_path` in `config.json` at the file you place here
(default: `models/human_detector.pt`). The path is resolved relative to the
`mavic_mission` controller folder.
